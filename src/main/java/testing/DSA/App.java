package testing.DSA;

/**
 * Hello world!
 *
 */
public class App {

	public static void main(String[] args) {
		System.out.println(String.format("%01d", 0100));
		/*
		 * 
		 * String s = "I Lo12ve m23y Cou45n56try"; boolean numFlag = false; int sum = 0;
		 * StringBuilder num = new StringBuilder(); for (int i = 0; i < s.length(); i++)
		 * { char c = s.charAt(i); if (Character.isDigit(c)) { numFlag = true;
		 * num.append(c); } else if (numFlag) { sum += Integer.parseInt(num.toString());
		 * num = new StringBuilder(); numFlag = false; } } System.out.println("Sum =" +
		 * sum);
		 * 
		 */}

}
